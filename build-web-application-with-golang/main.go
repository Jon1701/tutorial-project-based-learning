package main

func main() {
	// `make` does memory allocation for built-in models:
	//   - `map`
	//   - `slice`
	//   - `channel`
	//
	//  `new` does memory allocation for types
	//   - returns a pointer
}
